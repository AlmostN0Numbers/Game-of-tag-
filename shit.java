package tag;
import java.util.Arrays;
import java.util.List;

public class shit {
    public static void main(String[] args) {
        List<String> names = Arrays.asList("Luke", "Reid");
        TagManager tagManager = new TagManager(names);
    }
}